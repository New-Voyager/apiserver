scalar DateTime

type playerTrack{
  buyIn: Int
  stack: Int
  seatNo: Int
  noOfBuyins: Int
  hhRank: Int
  hhHandNum: Int
}

type Query {
  # playerGametrack(clubCode: String!, gameCode: String!, playerId: String!): playerTrack! # Resolver Not found?
  rakeCollected(gameCode: String!): Float!
}
