# graphql used for development

type Mutation {
  deleteGame(gameCode: String, includeGame: Boolean): Boolean
  debugHandLog(gameCode: String!, handNum: Int!): Boolean
  requestFeature(feature: String!): Boolean
  reportBug(bug: String!): Boolean
}
